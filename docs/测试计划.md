# 全量测试计划 / Full Test Plan

两期：一期 wire 层（已完成 ✅，可复验），二期端到端（需重启后，在 scheduler 会话执行）。

## 一期：中转 wire 层 ✅ 已完成（2025-08-14）

工具：[dsh-relay-compat](https://github.com/y08lin4/dsh-relay-compat) 的 `scripts/wire-test.ps1`
（`-Base` / `-KeyEnv` 参数化，任何人可跑；key 走环境变量不落盘）。

结果：

| 项 | 结果 |
|---|---|
| 7 档（none…max）× 2 模型（flash/pro） | 全 200 ✅ |
| `none` | think=no（关思考生效）✅ |
| 其余 6 档 | think=YES ✅ |
| `off` 直接发 wire | 400 ❌ → 证实 settings 的 `off:"none"` 映射是必需的，当前配置正确 |
| developer 角色 | 400 ❌ → 证实补丁 + `supportsDeveloperRole:false` 是刚需，均已就位 |
| 请求体体积 | 1024KB→200 / 1050KB→413（阈值 ≈1MB，服务端硬限制，DSH 侧无解） |
| 20 路并发 | 全 200 无 429 ✅ |
| 工具 schema 400 回归（A/B） | 旧写法（属性级 `required:true`）→ **400 精确复现**；v4 写法（对象级 `required:[...]`）→ **200** ✅ |

一期排掉的雷：flash 不支持思考参数？→ 支持；中间 4 档被拒？→ 全通；
20 并发触发限流？→ 无；v4 修复是否有效？→ wire 级 A/B 证实（旧写法 400、新写法 200）。

## 二期：端到端（重启后，在 scheduler 会话执行）

前置：**重启 DSH**（加载 v4 schema 修复与 7 档插件）。

- [ ] 1. 基础对话：无 developer 400、无 subagent schema 400
- [ ] 2. 主代理选择器：7 档（off/minimal/low/medium/high/xhigh/max）可见可选
- [ ] 3. 派 subagent `model=deepseek-v4-flash, effort=off`：成功返回、明显少烧 token
- [ ] 4. 派 subagent `effort=max`：成功返回、有深度思考痕迹
- [ ] 5. 派 subagent_fork：成功返回、能引用当前对话上下文
- [ ] 6. 跑一次 workflow（带 model override）：成功返回
- [ ] 7. 并发 20 个 subagent：全部完成，无 429 / 崩溃
- [ ] 8. 挂载 relay-doctor.js：启动日志两项 ✔（补丁 + 开关）
- [ ] 9. 长会话逼近 1MB 时的 413 表现（预期：触顶报 413，属服务端限制）

### 重点盯防（一期测不到的部分）

1. **effort proxy 链静默失效**：子代理不报错但档位没透传。第 3/4 步专门验证——
   `off` 的子代理若仍长篇思考、`max` 的子代理若秒回敷衍，说明
   agentOptions → reasoningEffort → 子代理请求 这条链断了，需要查
   `subagent-routed.v2.js` 的 ContinuableSetup 代理。
2. **model 参数路由**：`model=deepseek-v4-pro` 的子代理是否真跑在 pro 上
   （耗时/质量应有感知差异）。
3. **schema 修复后的真实派发**：第 1/3 步验证 v4 的 `required:["description","prompt"]`
   在真实模型调用中通过中转校验。

### 一期复验命令

```powershell
$env:LYAI_API_KEY = (从 ~/.dsh/.credentials.yaml 取 LYAI_API_KEY)
pwsh -File dsh-relay-compat/scripts/wire-test.ps1 -Base https://你的中转/v1 -KeyEnv LYAI_API_KEY
```
